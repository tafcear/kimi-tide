# 更新日志（CHANGELOG）

**月汐（kimi-tide）**是 DSH 的「每一步自动选模型」插件。

> DSH（DeepSeek Harness）是 DeepSeek 官方开源的 AI 编程智能体框架——在网页里跟 AI 助手对话干活，模型、工具、界面都以插件形式装卸（官方口号：Everything is a Plugin）。

装上月汐后：贴截图自动切到能看图的模型，写代码自动切到编码模型，闲聊翻译自动走便宜的模型——每次选了谁、为什么，面板上看得见。

## 项目演进

- **第一段（自研接入，0.1.x）**：当初 DSH 没有 Kimi 通道，自研 OAuth 适配器把 Kimi Code 订阅接了进来。
- **第二段（路由与评分，0.2.x–0.3.x）**：接进来之后，真正的痛点变成「哪个任务该用哪个模型」——于是有了双模型路由、能力评分和图像护栏。
- **第三段（收敛聚焦，0.4.x 起）**：官方生态原生支持 Kimi 接入后，自研接入层果断退役。月汐只做官方没有的事：路由、护栏、编排、观测——规则你来写，决策看得见。

> 本文件是版本历史的**唯一事实源**（用户视角）。维护者证据链（commit 锚点、验收记录）见 [docs/release-evidence.md](docs/release-evidence.md)。

---

## v1.1.0（2026-09-04）

评审流落地：把「评审」从「一句话切走整轮模型」变成「本轮照常干，干完自动请评审」。

- **关键词组可被评审流认领**：把 review 流的触发方式从「手动」改成「关键词认领」并指定一组关键词后，这组词的命中不再把整轮路由到评审模型——本轮照常按你的规则执行，轮末自动请评审模型出意见（每轮至多一次，60 秒有界，不拖慢本轮）。
- **评审意见以卡片上屏**：会话流里直接出现评审卡（标注评审模型与触发的流）；评审失败也有灰态兜底卡，不会无声消失。
- **`/kimi-tide review` 手动评审**：想评上一轮就发这条命令；没有可评的上一轮时会明说。
- **设置页配套提示**：被认领组对应的路由规则行置灰并注明「该组已被评审流认领，不再参与路由」；「试一句」预演会显示「轮末触发评审流」而不是切模型。
- **校验加固**：评审流不允许给评审模型配推理力度档位（评审调用恒不带 effort），保存这类配置会被拒绝。
- 已知边界：评审模型暂不可用时，认领组照常抑制对应规则，但那一轮不会自动评审——「试一句」会显式标注「评审流已认领但评审模型不可用」，不留无声盲区。

[Release v1.1.0](https://github.com/tafcear/kimi-tide/releases/tag/v1.1.0)

## v1.0.1（2026-08-31）

纯文档版本：安装与路由行为零变化。

- 项目文档全面重写：全新中文主文档 + 独立英文文档——3 秒看懂「这是什么」，10 秒看懂「对你有什么用」，快速开始全程白话。
- 新增本更新日志（版本历史单一事实源）与维护者证据链文档；README 路线图瘦身为当前版本一行，版本史与证据链各归其位。
- 新增两道文档门禁进 CI：版本三方一致性（CHANGELOG / README / 包版本）+ 全库文档链接检查。
- 经 Kimi 双轮独立评审闭环（存档 `docs/audit/2026-08-31-zero-basis-docs-review-round1.md`）。

[Release v1.0.1](https://github.com/tafcear/kimi-tide/releases/tag/v1.0.1)

## v1.0.0（2026-08-29）

首个大版本：关键词匹配升级 + 规则体系补全 + 品牌主题化 + 多 plan 配额合流发布。

- **关键词匹配更准**：英文关键词按「整词」匹配（`decode`、`barcode` 不再误中 code 规则），命中规则按特异度排序（命中词多者优先、带图恒最优先），还可配「最少命中词数」避免一句话误触发。
- **规则覆盖面扩到 7 组内置关键词**（代码/审查/写作/翻译/长文/数学/闲聊），词表可改、可自建新组。
- **推理力度（effort）可配置**：规则目标、预设默认、转述流视觉模型三处可选档位；模型不支持时自动剥离，不会报错。
- **设置卡片新增「试一句」测试器**：输入一句话，实时预演命中哪条规则、路由到哪个模型。
- **决策更好懂**：面板决策理由带命中词数（如「规则 code 命中 2 词」）；dock 布局重构为两行加三页签，决策区常驻。
- **多 plan 配额**：额度槽跟随当前命中的模型自动切换——Kimi 显示周配额/5 小时窗，GLM 显示 5h token 窗/7 天周窗；没有套餐的目标自动置灰。
- **辅助请求可改道**：会话标题等后台请求可指定模型，不再跟随主路由误打思考模型撞超时。
- **月汐品牌视觉**：紫主题贯穿明暗双主题，设置页月牙图标。

[Release v1.0.0](https://github.com/tafcear/kimi-tide/releases/tag/v1.0.0)

## v0.6.1（2026-08-23）

稳定性修复波。

- 多图消息的转述并发处理，等待时间不再按图数叠加。
- 配额轮询带超时与去重，配额接口挂起不再拖垮面板。
- 面板推送去重，会话日志不再按分钟膨胀。
- 路由决策与流事件按会话隔离，多会话互不串台。
- 新增 CI（push/PR 自动构建与测试）。

[Release v0.6.1](https://github.com/tafcear/kimi-tide/releases/tag/v0.6.1)

## v0.6.0（2026-08-23）

协作编排。

- 规则目标可以是「协作流」：预置图像转述流——能看图的模型先把图转成文字，便宜的文本模型接力作答，读图只付视觉那一次的钱。
- 带图会话不再整场锁死多模态模型：每张图独立跟踪（原生处理/已转文字/当无图）。
- 预设级带图兜底三态可选：锁存 / 盲答（当无图）/ 懒转述。

[Release v0.6.0](https://github.com/tafcear/kimi-tide/releases/tag/v0.6.0)

## v0.5.0（2026-08-21）

规则驱动路由。

- 命名预设（省钱/能力/可自建）+ 有序规则（带图/关键词组）：命中即路由，未命中走预设默认模型打底。
- 六维能力评分引擎退役——路由依据变成你能读懂、能改动的规则。
- 候选池即 DSH「Models」页挂载的全部模型，不限 Kimi/DeepSeek；未接入的目标自动跳过并在面板置灰。
- 旧版配置升级时自动迁移。

[Release v0.5.0](https://github.com/tafcear/kimi-tide/releases/tag/v0.5.0)

## v0.4.0（2026-08-20）

接入方式转正。

- 一把 Kimi Console API Key + DSH 官方「Models」页配置即可使用，不再需要 Kimi CLI 与 OAuth 登录态。
- 路由配置搬进 DSH「设置 → 月汐」，重启保持。
- 新增 GitHub Actions 自动发版（打 tag 即出 Release）。
- 此前 0.2–0.3 的双模型路由器与能力评分能力随本版首次正式发版。

[Release v0.4.0](https://github.com/tafcear/kimi-tide/releases/tag/v0.4.0)

## v0.1.3（2026-08-15）

- 初代 Kimi 接入通道：凭据门控 + OAuth 加固（该自研接入方式已被 0.4.x 官方路径取代）。

[Release v0.1.3](https://github.com/tafcear/kimi-tide/releases/tag/v0.1.3)

## v0.1.1（2026-08-15）

- 项目首个发布版本：Kimi Code 订阅接入 DSH。

[Release v0.1.1](https://github.com/tafcear/kimi-tide/releases/tag/v0.1.1)
